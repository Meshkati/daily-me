# Daily Me

Personal hydration tracker PWA. Offline-first, all data stays on your device.

Built with React, TypeScript, Vite, Tailwind CSS v4, Zustand, and IndexedDB.

`npm run dev` to start locally, `npm run build` to build for production.
